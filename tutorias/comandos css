gap :20px;
->dar um espaço entro objetos justify-content: center;
->alinha no centro justify-content: space-around;
->distribui o espaço de forma uniforme hover: quando o mouse passa por cima do objeto transition: 0.3s;
->faz a transição de um estado para outro trasnform: scale(1.1);
->aumenta o tamanho do objeto em 10% quando o mouse passa por cima opacity: .0;
->deixa o objeto transparente quando o mouse passa por cima id=identidade única do objeto (se chama com #nome do id) class=classe de vários objetos (se chama com .nome da classe) ordem de prioridade ID (#) ↓ Class (.) ↓ Tag (sem nada) height ->altura width ->largura background-image:url(img/banner.png) coloca a imagem na div background-size: cover;
faz a imagem cobrir toda a div margin-(top ou left) para mover a div margin:auto;
centraliza a div padding: 20px;
cria uma espaço entro o texto a as paredes da div border: 3px solid black;
cria uma borda na div box-shadow: 10px 10px 20px black;
faz uma sombra na div width Largura height Altura background Cor de fundo background-image Imagem de fundo background-size Tamanho da imagem border Borda border-radius Arredonda cantos margin Espaço externo padding Espaço interno display Forma como a div é exibida position Tipo de posicionamento top,
left,
right,
bottom Movem elementos posicionados transform Move,
gira,
aumenta ou diminui transition Anima mudanças box-shadow Sombra opacity Transparência z-index Ordem de sobreposição overflow Controla conteúdo excedente cursor Tipo do cursor 
display flex Ativa o Flexbox 

Flex-drection:row,colun;
 diz a direção da flex coluna ou row